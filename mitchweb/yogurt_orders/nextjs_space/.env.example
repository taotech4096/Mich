DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME"

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-secret"

# Seed admin account (used by scripts/seed.ts)
SEED_ADMIN_EMAIL="admin@example.com"
SEED_ADMIN_PASSWORD="replace-with-a-strong-password"

# AWS S3
AWS_REGION="us-east-1"
AWS_BUCKET_NAME="your-bucket-name"
AWS_FOLDER_PREFIX=""

# Client-side AWS URL helpers (admin product image upload page)
NEXT_PUBLIC_AWS_BUCKET_NAME="your-bucket-name"
NEXT_PUBLIC_AWS_REGION="us-east-1"
# Optional if using CloudFront or custom CDN domain:
# NEXT_PUBLIC_AWS_PUBLIC_BASE_URL="https://cdn.example.com"

# Optional Mercado Pago
MERCADOPAGO_ACCESS_TOKEN=""
MERCADOPAGO_PUBLIC_KEY=""
# Optional custom base URL if your webhook host differs from NEXTAUTH_URL:
# WEBHOOK_BASE_URL="https://your-domain.com"
# Optional header secret for webhook validation:
# MERCADOPAGO_WEBHOOK_SECRET="replace-with-random-secret"

# Secret used by recurring subscription runner endpoint:
CRON_SECRET="replace-with-random-secret"
